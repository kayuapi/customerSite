// import { selectOrderedProductDomain } from '../selectors';

describe('selectOrderedProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
