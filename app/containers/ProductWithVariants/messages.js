/*
 * ProductWithVariants Messages
 *
 * This contains all the text for the ProductWithVariants container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProductWithVariants';

export default defineMessages({
  orderQuantity: {
    id: `${scope}.orderQuantity`,
    defaultMessage: 'qty',
  },
});
