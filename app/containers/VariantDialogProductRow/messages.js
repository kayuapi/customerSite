/*
 * VariantDialogProductRow Messages
 *
 * This contains all the text for the VariantDialogProductRow container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VariantDialogProductRow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VariantDialogProductRow container!',
  },
});
